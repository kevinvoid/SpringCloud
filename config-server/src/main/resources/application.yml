spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kevinvoid/SpringCloud
          search-paths: /config-server/config-repo
          username: kevin
          password: yangpingan888@163.com
server:
  port: 7001
